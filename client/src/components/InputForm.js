import React, { memo } from 'react'

const InputForm = ({label, disabled, register, errols, id, validate, type='text'}, placeholder) => {
    return (
        <div>
            InputForm
        </div>
    )
}

export default memo(InputForm)